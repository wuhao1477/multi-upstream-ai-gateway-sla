# 决策确认记录（OPEN-ISSUES 评审结果）

| 项目 | 内容 |
| --- | --- |
| 确认日期 | 2026-07-22（16 项参数 + 5 条新需求）；**2026-07-26 追加 [ISSUE-004](./issues/ISSUE-004-dev-preflight-feedback.md) 开工前裁决 20 条**（见文末） |
| 确认人 | 项目负责人（Amsterlam） |
| 确认方式 | 对 OPEN-ISSUES 全部决策点逐项对话式确认；ISSUE-004 由 PM 逐条填决定 |
| 结果用途 | 作为 PRD v1.2 升版和开发计划的输入；ISSUE-004 裁决作为 **PRD v1.4 基线**与 M0 开工的输入 |

## 贯穿全部决策的一条总原则

**策略参数一律"建议值作出厂默认 + 设置页可修改"**：设置页必须明示每项策略的含义、用法和修改影响；测活预算、禁测活规则等关键策略的修改需二次确认（用户确认理解设计后才能保存）。以下逐项记录中"可配置"均指此模式。

## 第二部分：16 项业务参数确认结果

| # | 参数 | 确认结果 |
| --- | --- | --- |
| 1 | SLA 指标 | **改为可配置**：SLA 等级的数量、各项指标数值、统计窗口全部由用户自定义（圈住的渠道不同，给的 SLA 就应不同）；建议的金/银/铜三级数值仅作默认模板 |
| 2 | 冲突优先序 | **改为可配置**：每个等级的 SLA/缓存/成本取舍顺序由用户自定义；建议顺序（金 SLA>缓存>成本、银 SLA>成本>缓存、铜 成本>缓存>SLA）作默认 |
| 3 | 测活预算 | 采纳（全局≤2% 月费用+日封顶、租户≤2%、会话≤1 次、失败≤错误预算 10%）；可配置，修改需二次确认 |
| 4 | 测活记账模式 | 默认全部业务严格补偿；设置中可全局切换为"预算延续"（成本优先），由用户决定 |
| 5 | 余额安全垫 | 采纳储备公式和 24h/6h/1h 三档处置，可配置。**余额不做实时查询**；新增需求：渠道"余额不足"信号自适应识别框架——各渠道返回余额不足的方式/状态机不同，需多策略适配（正则检测错误文案中的"余额"类关键词、真实请求查询账户/Key 余量是否归零等），优雅识别后触发处置 |
| 6 | 禁测活清单 | **不采用四条件清单**。并发重试原则上全局禁止（浪费大）；测活权限通过**对外模型别名**控制：把 SLA 策略编进模型名（如 `gpt-5.5-sla-1`、`sla-a-gpt-5.5`），调用哪个别名就适用哪套策略（含是否允许测活、SLA 等级），别名可自定义 |
| 7 | 全渠道不可用 | 采纳（金排队 15s、银 5s、铜不等待；不偷换模型），时长可配置 |
| 8 | 数据白名单 | **取消**。SLA 网关不处理请求内容，默认所有渠道可接所有请求；PRD 中"数据许可硬过滤"降级为可选功能 |
| 9 | 币种统一 | 统一美元口径；**一期所有币种 1:1 折算，币种仅作名称**，不建汇率机制（2026-07-23 明确） |
| 10 | 数据查询频率与过期 | 采纳建议值（价格 6h/24h/48h；余额校对 5～15min/30min/2h；订阅每小时、人工 7 天）作默认；可配置并说明影响。余额查询仅作后台校对（见参数 5） |
| 11 | 样本门槛与冷却 | 采纳建议值（1h≥20 次或 24h≥100 次；观察期 30min 或 50 次；冷却 5min 翻倍至 4h）作默认；**必须可配置**——用 SLA 网关的渠道普遍不稳定，建议值不一定适用所有人 |
| 12 | 容量保留 | 采纳（金级 20%、测活≤5%、接管保留金级峰值并发 10%），可配置 |
| 13 | 订阅台账字段 | 采纳建议字段（月费、生效/到期、周期额度、支持模型、倍率、重置规则、续订、超额计费、共享账号/Key；每字段标来源和更新时间，人工 7 天过期），另加**用满倍率**和**实际倍率**两个计算字段 |
| 14 | 订阅月费分摊 | **双倍率设计**：① 用满倍率 = 周期额度÷费用÷天数×分组倍率（按额度用满计算的边际成本口径），用于**调度排序**；② 实际倍率 = 按实际消耗重算，用于**账务和报表**（期中预摊、期末重算记差异）。理由：订阅固定费已付，边际成本趋零，用实际均价会错误降权未用满的订阅渠道。例：用满 0.034/实际 0.045 的订阅渠道，调度优先级应高于 0.035 倍率的普通渠道 |
| 15 | 订阅倾斜三道闸 | 采纳：成本≤最优普通渠道 1.5 倍；单订阅渠道≤该模型总流量 40%；仅剩余有效期≤7 天且预计浪费≥周期额度 20% 时启动 |
| 16 | 告警分级 | 采纳（P1 15 分钟/P2 1 小时/P3 当日），分类和时限可配置 |

## 第三部分：5 条新增需求确认结果（随 PRD v1.2 补入）

| # | 需求 | 确认结果 |
| --- | --- | --- |
| 1 | 自研决策核心可靠性 | 采纳：决策延迟 P99≤50ms、多实例部署、整体不可用时返回明确错误且**禁止旁路直连上游**。⚠️ **原「可用性≥99.95%」已于 2026-07-25 撤回**——单机部署下 Caddy/宿主机/单 PG 为共享故障点，无法验证；改为**进程级冗余**承诺（[PRD FR-110](./PRD.md)） |
| 2 | 对外 API 协议 | **只支持 OpenAI 两种格式：Chat Completions 和 Responses**；不做 Anthropic Messages。流式、工具调用、多模态字段透传，协议兼容性列入验收 |
| 3 | 审计数据保存 | 决策快照和费用账本存**元数据级**、保留 180 天可查询；**正文/用户上下文/请求头/请求体一期一律不存**（防数据库无限膨胀，后续版本再评估） |
| 4 | 上游 Key 存储 | **一期明文存储，不加密、不强制轮换**；保留既有 FR-094（日志和界面不显示完整 Key）。P1 轮换通过 `PATCH /admin/keys/{id}` 替换 `secret` 完成，不提供独立 rotate/grace 路由 |
| 5 | 系统规模 | **峰值 100～1000 QPS**。技术验证和容量规划按此量级；连带要求自研核心多实例与压测按此设计 |

## 第一、四、五部分确认结果

| 事项 | 确认结果 |
| --- | --- |
| 问题 1-4 验证渠道名单 | 后续提供。上游渠道大多数为 NewAPI/Sub2API 二开。**新增调研输入**：参考开源项目 [all-api-hub](https://github.com/qixing-jk/all-api-hub)（已实现自动创建 Key、自动获取渠道模型、自动签到、自动获取 Key 分组），评估其采集方式可否复用——这提示 NewAPI 系站点的用户侧接口自动采集可行性可能高于原预判。见 ISSUE-002 |
| 第四部分 6 项技术假设 | 确认执行方式：**新建 issue，由 AI 用 Docker 实跑相关项目做实际调研验证**（不写业务代码）。见 ISSUE-001 |
| 第五部分 许可证 | 使用场景为**个人/内部使用、不对外**：许可证风险基本解除，无需法务复核；Aether 保留备选清单；AGPL 项目无对外服务开源义务问题 |

> ✅ 2026-07-23：以下影响已全部写入 [PRD v1.2](./PRD.md)。

## 对 PRD v1.2 的主要影响汇总

1. 新增"策略配置化"总需求：SLA 等级体系、冲突优先序、测活预算、数据时效、样本门槛、容量保留、告警分级均可配置，关键策略修改需二次确认。
2. 新增"对外模型别名即策略载体"需求：别名映射 SLA 等级与测活资格；并发重试默认全局禁止。
3. 新增"渠道余额不足信号自适应识别"需求（替代实时余额查询）。
4. 新增"订阅双倍率"需求：用满倍率参与调度排序，实际倍率用于账务。
5. 数据许可硬过滤（FR-001～006 相关条目）降级为可选功能，默认全允许。
6. 明确对外协议为 OpenAI Chat Completions + Responses。
7. 明确一期不存请求正文/上下文/头/体；账本元数据保留 180 天。
8. 明确一期 Key 明文存储；规模假设 100～1000 QPS。
9. 补入第三部分 5 条需求的确认版本；16 项参数以本记录数值为默认值写入。

---

## ISSUE-004 开工前反馈裁决（2026-07-26，PM 确认）

> 来源：[ISSUE-004](./issues/ISSUE-004-dev-preflight-feedback.md)（开发接手前通读全量文档后的 20 条反馈）。**全部采纳**；落地时对其中 6 处按审计结果做了修正，见该文档 §0bis。本节是决议的**权威摘要**，细节与修正理由以 ISSUE-004 为准。

1. **单级 SLA ＝ 单一承诺等级**：金银铜保留为默认模板种子，`sla_targets` 只配一档；调度 / canary / 测活判断改读 `routing_policies.is_committed`、`canary_eligible`、`probe_allowed` 显式字段，**不读等级名**，二期加等级零改结构。
   ⚠️ **互斥口径以落地版为准**：CHECK 约束是 `NOT (is_committed AND (canary_eligible OR probe_allowed))` —— 承诺别名不进**任何**实验通道；`canary_eligible` 与 `probe_allowed` **可以并存**（非承诺别名两条验证轨都能用，M0 种子即如此）。原决议只写 `NOT(canary_eligible AND is_committed)`，那样承诺别名仍可能被主动测活选中。
2. **`/v1/models` 由网关合成**，不透传上游目录；非别名 404、越权 403、`x-models-etag` 我方按别名表版本自生成并支持 `If-None-Match → 304`；上游 `Models()` 降级为探测 / 管理用途。**字节透传硬约束的适用范围限定为 SSE 响应流保真**，不含控制面元数据（这是显式推翻 [03 §4](./dev/03-upstream-layer.md) 契约表的一条，已在该表标注废止）。
   📌 **后经实测修正风险方向**（[`verify/probe_codex_wire.py`](../verify/probe_codex_wire.py)）：Codex **不从 `/v1/models` 取模型名**，304 也非必需；真正风险是它原样发送自己 config.toml 里的模型名 → **接入前必须把 Codex 的 `model` 改成我方别名，否则 404**。该项已写入 [06 §7 接入前置](./dev/06-deployment-and-operations.md)。
3. **缓存目标拆两层**：对外一期不承诺（≥90% 推二期）；内部切换抑制目标默认 **0.6** 可配。
   ⚠️ 单一来源统一为 `config_params['cache_switch_min_hit_rate']`——原决议说"02 §6quater 不动"做不到：该节原文从 `sla_targets` 按 `sla_level` 查，既与 `config_params` 冲突，其 `WHERE sla_level=` 也与"不读等级名"冲突。
4. **FR-060 修订**：测活允许"贴近真实负载的预置模板"（多轮上下文、工具定义、典型长度，运维维护），禁 hello/ping；有业务流量时 canary 优先。依据：FR-112 禁存正文 → 无流量时段本无真实正文可复用。模板保真度低于真实请求的代价**已明示并签收**。
5. **canary 小时上限口径＝per-binding**（FR-121 措辞改"每路由资源（binding）每小时"）；渠道级总闸推二期、默认不启用。
6. **T1 Codex 实机实验＝M0 期间独立 spike**，timebox 2~3 天，**不进 M0 验收门禁**，产出回填 [13 §1](./dev/13-research-reassessment.md)；上游波动跑不通则记录阻塞、不拖 M0。
7. **补齐 7 项设计缺口**：内置 `system-probe` 探测凭证（不可外呼、不可被吊销/轮换/删除误伤、消费单独归因）；账号 / binding / 故障域三处停用列；容量保留＝登记上限＋DB 原子计数、**未登记即不保留**（`/admin/bindings` 须显式提示"保留未生效"）；计费容差默认 5%（关键项、走二次确认）＋ `GET /admin/ledger/reconciliation`；Prometheus `/metrics`（仅内网、profile 可选、默认不启动）；`alert_events` 写入路径提前到 M3；一期 **user ≡ gateway_client** ＋ Caddyfile 不代理 `/admin/*` 与 `/metrics`。
   ➕ **另补决议未覆盖的 4 项**：`system-probe` 的 `rpm_limit` 等三项属性（不设会让探测风暴无上限）；FR-063 五维预算一期实际只有 global / binding / client 三个独立维度；AC-18 须以"已登记容量"为前置否则无从验证；webhook 失败计数须进 `/metrics`，否则"仅记日志不重试"就是静默失败。
   ⚠️ 计费容差公式补下限保护：`abs(cost_variance)/estimated_cost` 在 `estimated_cost=0` 时除零（免费渠道／估算失败／0 token），低于 $0.01 改用绝对差额判定。
8. **PRD 升 v1.4 并冻结基线**，同步 00/02/03/05/06/09/13/14/15 与 6 处文档残留；**D1/D2/D3/D5 风险签收**（同步写性能待 M4 压测冻结阈值、唯一许可优化＝组提交；REAL 类外站波动按重试窗口处理；币种 1:1 后果确认；AC-09 口径按"接管成功→仅扣探索预算、接管失败→两边都计"对齐）；**D4 最小告警 webhook 拉入 M4**（P1 单条 POST，失败仅记日志不重试、不阻塞落库，失败计数进 `/metrics`）。

---

## ISSUE-005 交付切分裁决（2026-07-27，PM 确认）

> 来源：[ISSUE-005](./issues/ISSUE-005-phase1-upstream-inventory.md)。触发原因：整体交付体量过大，负责人指示先只做上游采集与管理。**11 条决定全部采纳推荐方案**。本节是决议的**权威摘要**，细节以 ISSUE-005 为准。

1. **术语分层，不做全局重编号**（P0）：「一期 / 二期」自此**只表需求范围**（一期＝最终目标内必做、二期＝可推迟），**交付进度另立 P1~P4**。
   - **最终目标** ＝ 原一期 31 条 AC + 原二期 5 条 + P1 新增 4 条 ＝ **40 条 AC**。
   - **既有 249 处「一期」/ 82 处「二期」一字未改**——包括本文档的历史确认记录。理由：改它等于篡改 PM 逐项确认的结果；且"一期 vs 二期"的需求范围划分本身依然有效（订阅制确实推迟）。
   - 落地只两处：[PRD §2.1.0](./PRD.md) 加交付进度表 + [00 §3](./dev/00-overview-and-milestones.md) 加阶段对应表。
2. **P1 ＝ 多上游渠道采集与管理**，**完全不含请求转发**（不碰 `/v1/*`、账本、调度、SLA）。原 M1+M2 → **P2**、原 M3+M4 → **P3**、原二期 → **P4**；**三者的里程碑内容、退出标准与验收判定一字未改**。P1 只依赖 M0，可独立交付独立验收。
3. **充值倍率 P1 不做**（指示①：多数站点 1:1、少数 1:2）。理由不只是比例——它**唯一的消费者是成本排序**，而 P1 没有成本排序。
   ⚠️ **登记为 P3 必做**（ISSUE-005 §6 T-1）：不做则 1:2 充值渠道的真实成本被高估 2 倍、永远选不上，与 [PRD §1.3](./PRD.md)「降低实际成功成本」直接冲突。
4. **数据库按 ponytail 决策阶梯做最小设计**（指示②）：新增 **3 张表**（`channel_groups` 6 列 / `group_models` 3 列 / `channel_model_catalog` 6 列）+ `upstream_keys` **补 6 列**。
   **刻意不建的 11 个字段**（P1 无消费者，需要时 `ALTER ADD COLUMN` 无损）：高峰倍率四字段、独占标记、平台归属、自报能力位三项、`enabled_model_id`、`display_name`。自报能力位另有一层理由——P2 本就要用 `Probe()` 实测（[03 §8](./dev/03-upstream-layer.md)），现在采自报值等于存一份将被推翻的数据。
   Key 用量**历史**复用已有 `collector_snapshots(scope_type='key')`，**不建新表**。
5. **补齐三处此前无载体的能力**（指示③）：① 全库没有 group 实体——分组只是 `multiplier_versions` 的一个数字，[04](./dev/04-collector-adapter.md) 的 `Group` 结构采回来无表可落；② `upstream_keys` **没有任何余额/用量列**，而 04 §4 声称 `FetchKeys` 写它（**既有错误**），"这把 Key 还剩多少"查不出来；③ 渠道模型目录只能进强制 token 上界的 `models` 表，20 渠道×200~300 模型不可行。
6. **新增 FR-122~128**（Key 生命周期、分组实体、分组可用模型、Key 用量、渠道模型目录、Key 级限流**只登记不判闸**、手动刷新）与 **AC-37~40**；合计 AC 36 → **40**（一期 31→35）。
7. **「实时更新」的可实现形态**：上游无变更推送 → 不做推送式实时。`POST /admin/channels/{id}/sync` 手动立即刷新（须限流防风控）+ FR-116 周期采集，界面显示 `synced_at` 与陈旧标记。
8. **P1 交付 10 个管理端点**（[09 §5.0](./dev/09-admin-api.md)）：渠道/账号/Key CRUD、Key 停用（轮换通过 `PATCH /admin/keys/{id}` 替换 `secret` 完成，不设独立 rotate/grace 路由）、Key 用量历史、分组与分组模型、渠道目录、手动刷新、资产总览。**P1 不新增任何 `/v1/*` 端点**；入站凭证 `gateway_clients` 属网关，P1 不做。
9. **另登记 5 项后续阶段任务**（ISSUE-005 §6）：T-2 `capacity_claims` 按 Key 归集、T-3 限流三层 MIN 派生、T-4 高峰倍率等字段回填、T-5 模型"启用"动作、T-6 余额信号自适应识别。
   ⚠️ **T-2 是既有缺陷、非本次引入**：`capacity_claims` 按 `binding_id` 计数，而同一把 Key 下 N 个 binding 共享上游配额，各自判闸会放行 N 倍 —— **容量保留在多模型渠道上目前实际失效**。P3 上容量保留时必修。
10. **PRD 升 v1.5 并冻结**；同步 00/02/04/09/14/15 与 `verify/check_docs.py`（AC 计数 36→40）。
11. **落地时另修 3 处工具与文档缺陷**（非决议内容，实施中发现）：① 列引用检查只认 `CREATE TABLE`，一条 ALTER 加多列时后 5 列被误报为"不存在"；② 悬空列检查把 `docs/issues/` 也算作规则载体，导致"建了表但规则没写"可静默通过（实测 `group_ref`/`first_seen_at` 曾这样蒙过）——已收窄为只统计 dev/ + PRD；③ 收窄后立即抓出真实悬空列 **`attempts.gateway_status`**（原 beta5 存证列，C6 只改注释未删列，全库无任何规则读写）→ **已删除**。
