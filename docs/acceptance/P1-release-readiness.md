# P1 发版就绪评估

| | |
| --- | --- |
| 评估对象 | 分支 `feat/p1-upstream-inventory`，68 个提交 / 167 文件 / +25025 −2301（对 `main`） |
| 评估日期 | 2026-08-30 首版；2026-08-31 复评（`b2ba4ad`）；2026-08-29 修掉 §3.3 表定义缺陷与 §3.4 处置后再全跑（`7be34a7`→`340a048`）；2026-09-01 第一轮：sqlc 翻案落文档后整列重跑于 `a321e73`（[§2](#2-验收规模全部实跑非推断) 那张空档表的第二行就是这轮抓出来的）；**2026-09-01 第二轮**：自审 + Codex 三次评审共六条缺陷全修后整列重跑于 HEAD `97fb4ab`，并**把退出标准①从 ✅ 改判为"不可判定 → 未通过"**（[§1](#1-四条退出标准的判定)）；**2026-09-01 第三轮**：这轮审的是"有没有过度设计"（[附三](#附三2026-09-01-第三轮--这次审的是有没有过度设计)），删掉 −40 行后又自己撞出**三条**验证链缺陷（改已应用迁移的注释锁死启动、PG 宿主端口落在临时端口段导致 CI 假红、Chrome 启动只等 puppeteer 默认的 30s），整列重跑于 HEAD `2b77241` |
| 判定依据 | [00 §3 P1 行](../dev/00-overview-and-milestones.md) 的四条退出标准 · [#13](https://github.com/wuhao1477/multi-upstream-ai-gateway-sla/issues/13) 退出清单 · [14 §3](../dev/14-acceptance-matrix.md) 规则 3「不可判定即不通过」 |
| 证据 | [P1-evidence.md](P1-evidence.md)（本文件不重复举证，只做判定） |
| **结论** | **技术上可发；但退出标准①按本仓库自己的规则未满足，所以"发不发"是运营决定，不是这份文件能下的判定**（2026-09-01 改判，见 [§1](#1-四条退出标准的判定)）。①的 AC-38 在 sub2api 一族上**不可判定**（那 13 个渠道已放弃纳管，HEAD 上跑不了），而 14 §3 规则 3 写着「不可判定即不通过」；②③④ 可复跑且全绿。§3 那**六条**已知缺口照旧一起发（§3 共列八条，§3.2 与 §3.6 已结清；§3.8 是 2026-09-01 第三轮新增：72 个配置键里 68 个没接线）。§4 三个待决事项**全部已决定并执行**。三轮外部/自我评审的处置：2026-08-29 复评两处（标准③在 CI 里零覆盖 → **已补守卫**；文档写着 `pgx + sqlc` → **已按 A 结清**，§3.6）；同日 Codex 二次评审两条 [high]（导入半成品 **已修**；SSRF 边界**部分修**，§3.7）；**2026-09-01 自审 + Codex 三次评审共六条，全部已修**（[附二](#附二2026-09-01-自审--codex-三次评审的六条)），其中两条**证伪了 §3.7 原先"校验已收成一处"那句话** |

> 这份文件此前不存在。2026-08-30 的 P1-evidence §5.13 记着一条我自己的记账错误：
> 上一轮我声称写下并提交过 `P1-release-readiness.md`（commit `7c4c72f`），实测
> `git cat-file -t 7c4c72f` 报 `Not a valid object name` —— 那份评估只在对话里给过。
> 本文件是那次声称的实物。**"我记得做过"不是证据。**

---

## 1. 四条退出标准的判定

| # | 标准（00 §3 原文） | 判定 | 依据 |
| --- | --- | --- | --- |
| ① | **AC-37/38/39/40 全通过** | ⚠️ **不可判定 → 按 14 §3 规则 3 记为未通过**（2026-09-01 改判） | AC-37/39/40 在 HEAD 可复跑且全绿；**AC-38 的"每族都跑过"不能**：sub2api 那一族只有 2026-08-29 的归档，而那 13 个渠道已全部放弃纳管，HEAD 上无法再跑一次。详见下方改判说明与 §3.1 |
| ② | 对**全部真实渠道**跑一次全量 sync 并留存覆盖率报告 | ✅ | 四轮已归档进 [`coverage/`](coverage/)。前三轮 65 渠道逐站有结果；2026-08-31 那轮是 45 渠道（放弃 20 个后的在纳管范围），跳过的 20 个记在 `skipped_disabled` 里 |
| ③ | Key 明文在响应/日志/抓包中**一处都不出现** | ✅ | 整份 DOM grep（FR-094）+ core 日志断言（`ui-stack.sh` 末步）+ 脱敏在 SQL 侧（`left(secret,8)\|\|'…'`，完整 secret 不出库）+ **CI 侧 `secret_render_test.go` 三条源码断言**（2026-08-29 补，见下） |
| ④ | **每个已注册家族**的 `Capabilities()` 与实际返回一致 | ✅ | `TestCapabilitiesMatchDocMatrix` + [§5.14](P1-evidence.md#514-加一族更可靠这个声称本身的验证2026-08-30) 的注入验证（往注册表塞一族假家族，五种填错各红一次） |

⚠️ **标准①从 ✅ 改判为"不可判定即未通过"（2026-09-01，采纳 Codex 三次评审的 [high]）。**

改判的不是证据，是**判定规则怎么用在自己身上**。原先那行写着 ✅，附一句"靠归档举证、
不可复验、判定基准未改" —— 也就是**同一格里既说通过、又说不可判定**。而
[14 §3 规则 3](../dev/14-acceptance-matrix.md) 是本仓库自己定的：**不可判定即不通过**。
那条规则写下来就是为了防"做过一次、之后信我"，AC-38 的 sub2api 侧恰好是它的靶心：

| | |
| --- | --- |
| 现在能跑的 | AC-37 / AC-39 / AC-40，以及 AC-38 的 newapi 一族（HEAD 上可复跑） |
| 现在跑不了的 | AC-38 的 **sub2api** 一族 —— 13 个渠道 2026-08-31 全部放弃纳管（§4 ①），真库里已无该族在纳管渠道 |
| 唯一证据 | 2026-08-29 的归档。**它是真的**，但它证明的是"那天通过了"，不是"HEAD 通过" |
| 按规则的结论 | 不可判定 → **未通过** |

**这不改变任何一行代码，也不推翻那份归档** —— 改的是这张表不再用一个 ✅ 掩盖
括号里的免责声明。谁读到 ✅ 就不会读那句话，而那句话才是实情。

**发不发版是你的决定，不是这份文件的。** 技术面见 §5；这里只负责让①不再自称通过。
恢复①需要一件具体的事：真库里重新有一个在纳管的 sub2api 渠道并跑通 AC-38 ——
那与"放弃纳管 20 个站"这个运营决定直接冲突，所以它不是一个待办，是一个取舍。


**2026-08-31 已结清，但结清方式是"这些渠道不再纳管"而不是"每个都指了人"。**
运营决定三类（需人工重登 / 形态不符 / 凭证失效）全部放弃，20 个渠道已停用并带原因
（[P1-manual-channels.md](P1-manual-channels.md)、[§5.18](P1-evidence.md#518-放弃-20-个采不到的站并修掉停用不影响采集2026-08-31)）。
于是"责任人"这一列失去对象 —— 没有需要人去维护的站了。

⚠️ 这与 §3.1 那次「第三族被移除而非填上」是同一种结清：**放弃是一个决定，不是一次修复。**
那 20 个站的真实可采集性至今未被恢复过，只是不再由本系统承担。

⚠️ **标准③的判定在本轮复评里变过一次。** 原先那三个依据**全部只在本地可跑**
（两条在需真上游的 58 项里、一条需内网真库），而 [#9](https://github.com/wuhao1477/multi-upstream-ai-gateway-sla/issues/9)
的完成标准写着「CI 脱敏断言覆盖新端点」—— 那一条从未落地，即标准③在 CI 里**零覆盖**。
已补 `internal/store/secret_render_test.go`（读路径不选明文 / 脱敏表达式真截断且长度 ≤8 /
明文回显恰好一处且在 `rotateKey` 内），5 路反向自验各红在自己那条断言上。
补的过程里头两条断言"形状对、范围错"，干净树上全绿却抓不到 `k.secret` 与
`left(secret,32)` —— 记在 [§5.21](P1-evidence.md#521-key-明文那条退出标准在-ci-里零覆盖而补上的守卫头两条是空的2026-08-29)。
**源码断言挡不住"取了前缀又在别处打日志"**，那一层仍只有本地那三条 grep。

### 1.1 #12 / #13 的关闭口径（2026-08-31，2026-09-01 收紧）

两个 issue 的退出标准**有两项不能直接打勾**：第四项要带注明，**第一项 2026-09-01 起
改成不勾**。

| | |
| --- | --- |
| 原文 | 「采不到数据的渠道**列出人工维护责任人**」（#12 完成标准 4 / #13 退出标准 4） |
| 实际做的 | 把这 20 个站**移出纳管范围**（`status=disabled` + 停用原因） |
| 为什么不直接打勾 | 打勾会让日后读 issue 的人以为**存在一份责任人名单** —— 那份名单不存在，而 [`P1-manual-channels.md`](P1-manual-channels.md) 的标题已经是「已放弃纳管」 |
| 勾选写法 | `[x] …（以放弃纳管方式结清，非指派责任人）` |

**#12 第一项（AC-37/38/39/40 全通过）：保持不勾。** 原先的写法是"打勾 + 注明
sub2api 侧靠归档举证"，2026-09-01 随标准①的改判一并收紧 —— **勾上就等于声称它
在 HEAD 上通过了**，而它不可判定。注明写在勾旁边不管用：读 issue 的人先看到方框里
那个 `x`。这与 §3.6 末尾那两个 `sqlc generate` 复选框保持不勾是**同一条判据**：
复选框是一句断言，不是一个进度条。判定基准（`collector.All()` 的每一族都跑过）
**一个字不动** —— 改的是勾不勾，不是标准本身。

**关闭 ≠ 缺口清零。** 本文件 §3 那六条缺口在 issue 关闭后依然成立 —— 而已结清的 §3.6
（文档写着 sqlc 而仓库里没有）**正是从两个已关 issue 的未勾选完成标准里翻出来的**。
其中两条在 2026-08-29 有了进展：§3.3 的**表定义**部分已修（迁移 018 + `test-migrate.sh` 第 6、7 步），
但成本公式那条 CI 断言仍欠、`per_call` 与 NULL 两支公式仍未实现，**P3 动成本计算前必须补**；
§3.4 的处置已定（停用 + 改名），但那 3 条 Key 断言**仍然跑在夹具上**，这一点没变。

## 2. 验收规模（全部实跑，非推断）

下表**整列都在 `2b77241`（当前 HEAD）上重跑过**（2026-09-01 第三轮；此前是同日的
`97fb4ab` 与 `a321e73`、2026-08-29 的 `7be34a7` 与 `340a048`、2026-08-31 的 `b2ba4ad`）。复评每轮
先做的都是核对"每份证据取自哪个提交"，而这条纪律**已经抓出过两次同类空档**：

| 何时 | 空档 | 怎么补 |
| --- | --- | --- |
| 2026-08-31 | 最后一次动产品代码的 `97a8caa` 只带了 `make check`，58 项上一次跑在它前一个提交 —— 而它改的正是界面「手动采集」那条采集路径 | 在 HEAD 上重跑，见 [§5.17](P1-evidence.md#517-全套件在-head-上重跑一遍并补掉真库套件的生命周期2026-08-31) |
| **2026-09-01** | **我自己刚犯了同一件事**：上一版把基线写成 `340a048`，随后 `722578b`/`987c8b7` 又动了 **8 个产品文件**（`import_api.go` +133 行、`credentials.go` 的 `Save`/`SaveTx` 拆分、`store/pool.go` 的 `DBTX`、`main.go` 的注入点），那句"整列都在 `340a048` 上重跑过"在 `722578b` 落地的那一刻就过期了 | 整列重跑，基线改为 `a321e73` |

⚠️ **第二次是"写下基线之后又改代码"，不是"忘了跑"** —— 比第一次更难自查，因为
文档当时是对的。教训：**动产品代码就得连着改证据基线那一行**，否则那行会自己变成
一句过期的真话。

**这一轮是那条教训第一次正向生效**：[附二](#附二2026-09-01-自审--codex-三次评审的六条)那六条修复动了
5 个产品文件 + 1 个迁移，于是**基线与代码在同一轮一起改** —— 整列重跑于 `97fb4ab`，
表里没有一行是从 `a321e73` 那轮抄下来的。没有第三行空档要补，这本身就是这张表存在的理由。

**第三轮又改了一次基线，而且是当天第二次**：[附三](#附三2026-09-01-第三轮--这次审的是有没有过度设计)的过度设计清理
（4 个产品文件）＋随后自己撞出的三条验证链缺陷（1 个迁移文件回滚 + 3 个验收脚本 + 3 个
浏览器脚本），把 `97fb4ab` 那句话又变成了过期的真话。整列因此重跑于 `2b77241`。
⚠️ **这不算第三行空档** —— 空档的定义是"文档里那句话已经过期而没人改"，
而这两次都是在同一轮里连着改的。判据仍是下面那条命令，不是我的记性。

**基线 `2b77241` 之后的提交只有文档**，判据是一条可复跑的命令（空输出即成立）：

```bash
git diff 2b77241..HEAD --stat -- '*.go' '*.sh' '*.mjs' Makefile deploy/ web/ migrations/
```

这么写而不是"再改一次基线号"，是因为每加一段文档就重跑一遍 107 项没有意义 ——
但**判据必须可复跑**，否则"只改了文档"又是一句只能靠我记性的话。

⚠️ 历史注记：实库迁移（018）与实库写入（`channel 1` 的 PATCH）都在 `340a048` 那轮
之前完成，故 35 项自那轮起跑的都是改后的台账（44 在纳管 / 21 停用）。
**迁移 019 在本轮落到内网真库**（18 → 19），落之前做了只读预检：65 行里原样/去尾斜杠/
转小写三种口径下重复均为 0、非法形态 0、带尾斜杠 0；回退是一条 `DROP CONSTRAINT`。

| 套件 | 结果 | 能否进 CI |
| --- | --- | --- |
| `make check`（fmt / vet / **test -race** / 配置键生成 / 迁移一致）+ 本地 `golangci-lint`（**不含在 `make check` 里**，CI 的 `gate` 才跑） | ✅ | ✅ |
| `verify/gate.sh`（文档 13 类 + 覆盖率分类自测 + DDL 真跑 pg16，109 条） | ✅ | ✅ |
| `test-migrate.sh` **8/8**（第 6、7 步：站型注册表一致 + 两处 `billing_unit` 列定义一致；**第 8 步是渠道与导入写路径的 8 条真库 Go 测试**，两轮各一批，见[附一](#附codex-二次评审的两条-high2026-08-29)与[附二](#附二2026-09-01-自审--codex-三次评审的六条)） · `test-config-api.sh` 9+2 · `test-compose.sh` 5 步 | ✅ | ✅ |
| `ui-stack.sh`：SPA **14** + 浏览器 **58** + 日志 **1**（真上游取自探活：redacted-channel-03 API `upstream-a.invalid`，1370 个模型 / 倍率 1161 + 按次 209；导入试运行 106 条 → 可入库 67 / 跳过 39 / 失败 0） | ✅ | ❌ 需真上游令牌 |
| `remote-stack.sh`：内网真库只读 **35**（2026-08-31 从 32 增：停用态 3 条），本轮在**应用了 019 之后**跑 | ✅ 35/35 | ❌ runner 到不了 <internal-db-host> |
| `test-arm-cloud.sh`：云端 arm64 真机（3 个 ELF 均 arm64 / 镜像版本 = HEAD / 其上再跑 14+58） | ✅ 镜像 `sla-core:arm64-2b77241`，云端 run #33439527246 | ❌ 需 arm64 宿主 + 真上游 |
| `secret_render_test.go` **3** 条（2026-08-29 补：标准③的唯一 CI 侧守卫，5 路反向自验；**含在 `make check` 的 `test` 里**，不另计入 107） | ✅ | ✅ |
| CI `gate` + `build-arm` @ `2b77241` | ✅ run #33439527390 / #33439527246 | — |

⚠️ 两处**活数据波动**，都不是缺陷：① 批量导入试运行本机 67/39，2026-09-01 前一轮
在 arm64 上是 66/40 —— 有个站在两轮之间挂了；② 真库目录模型数 1369 → **1370**。
断言查的都是"两类被区分开"/"上千行量级"而非某个数字，所以各轮都绿。
**这正是"靶子别写死"的理由。**

合计 **107 项**（= 14+58+35 三份浏览器套件）+ 1 条日志 shell 断言 + 上表其余 shell/Go
门禁。**其中 93 项只能本地跑** —— 真上游令牌不进
GitHub secrets 是明确决定（第三方真凭证，fork 触发的 `pull_request` 与构建日志都会漏），
代价是 CI 只能当免密回归网。见 [CLAUDE.md §1 的后果表](../../CLAUDE.md)。

## 3. 必须一起发的六条已知缺口（§3.2 与 §3.6 已结清，标题保留）

发版不等于没缺口，等于**缺口都被写下来且都不致命**。逐条给出"为什么不阻断发版"。
已结清的两条**不删**：删了就读不出"欠账是怎么消失的"，而那比"欠账是什么"更值得留。

### 3.1 AC-38 的"两族都跑过"证据只剩首轮，且今后不会再有新的

> **2026-08-31 这条缺口升级了，当时按"认首轮归档为终局证据"结清；
> 2026-09-01 撤销那次结清 —— 归档不能替 HEAD 作证。**
> 放弃那 20 个站时，三类"需人工"恰好装满了**全部 13 个 sub2api 渠道**，
> 于是真库里 sub2api 的在纳管渠道数为 **0**：判定基准（每个已注册家族都跑过）
> 与真库现状不可能同时满足，本条从此**不可复验**。
>
> 处置：**判定基准一个字不动**，但 AC-38 与退出标准① **一并记为"不可判定 →
> 未通过"**（[§1](#1-四条退出标准的判定)）。另两条出路（改判定基准 / 留一个注定
> 失败的标本）分别被 [14 §3](../dev/14-acceptance-matrix.md) 规则 3 和「只保留可用的」
> 这个决定排除，排除依据逐条写在
> [P1-evidence AC-38 那段 ⚠️](P1-evidence.md#ac-38-逐家族手动刷新逐项结果与-unsupported-显式上报)。
> 将来接入任一 sub2api 站点，本条自动重新可验。
>
> ⚠️ **"认归档为终局证据"这个处置本身是错的**（2026-09-01 由 Codex 三次评审指出）。
> 它绕开了同一份 14 §3 规则 3 —— 那条规则不只禁"改判定基准让它变绿"，也禁
> "不可判定却记成通过"。当时写的是 ✅ 加一句免责，而**读 ✅ 的人不会读那句免责**。
> 归档没有被推翻，它证明的仍然是"2026-08-29 那天通过了"；错的是拿它当 HEAD 的判定。

2026-08-30 复测时 13 条 sub2api **全部 401**，`matrix` 里 `sub2api|*` 六项一条不剩
（[§2.1bis](P1-evidence.md#21bis-次轮复测sub2api-掉到-0-可用ac-38-的两族都有真实结果只对首轮成立)）。

**不阻断**：退的是别人家站点的凭证有效期，不是代码 —— 两轮之间 `internal/collector`
只有一处改动（加 `TokenExpiryFrom`），它不会让原本能过的站变 401。判定基准
（`collector.All()` 每族都跑过）在首轮那份归档里成立，且该基准本身有单测守着。
**但要接受**：今天重跑，sub2api 侧拿不到新证据。

2026-08-31 复评补了一条**旁证**（不是这条缺口的解）：`97a8caa` 之后在 HEAD 上重跑
58 项，newapi 系那条采集路径六项仍全绿（`account`/`groups`/`keys`/`model_catalog` ok +
`subscription_quotas` unsupported + 限流 429）。所以「改采集代码把站弄坏了」这个
可能性被排除了；sub2api 那 13 条仍是凭证到期，不是代码。

### 3.2 ~~那 20 个站里 11 条永远不会自愈~~ → 已按"放弃纳管"结清（2026-08-31）

> 这条不再是缺口：20 个站已全部停用，不在纳管范围内。保留标题是因为下面那段
> 说明了"为什么修复不会让它们好起来"，而那正是放弃它们的依据。

`RefreshLead` 自锁修好之后（[§5.15](P1-evidence.md#515-主动续期从来没有触发过本轮发现的真缺陷已修)），
11 条 sub2api 报 `无 refresh_token 可用（需人工重登）`。库里 0 个 refresh_token、
0 个 password，**修复不会让它们好起来** —— 改善只在于失败从静默变成具名。

**不阻断**：P1 不转发请求，采不到的渠道只是台账缺行，不影响任何在线路径。
恢复路径存在且有断言（04 §5.3 账密重登）。

### 3.3 `billing_unit`：~~权威表的列定义本身是反的~~ → 已修；仍欠 P3 那条计算断言

⚠️ 2026-08-31 复评发现问题不在 CI 而在**表定义**：`price_versions.billing_unit` 是
`NOT NULL DEFAULT 'per_1m_token'` **且无 CHECK**，而目录表可空 + 有 CHECK ——
**不设防的那张恰是 P3 要读的权威表**。015 想禁止"把未声明伪装成按 token 计价"，
006 的列定义强迫写入路径这么做。

**已于 2026-08-29 修**：迁移 018 去掉默认值与 `NOT NULL`、补一条与目录表取值逐字相同的
CHECK，`sink.go` 的 `COALESCE` 换回 `NULLIF`；守卫落在 `test-migrate.sh` 第 6、7 步
（断言"两处列定义相等"，另有真写入探针），反向自验 6 路全红。⚠️ 当轮跑的是 7/7，
第 8 步（导入原子性）是同日晚些加的，此后全套是 8/8。实库已应用（`applied=1`，
`price_versions` 仍 0 行，改表零风险）。详见
[§3.2bis](P1-evidence.md#32bis-两张表对同一个口径的处置相反而不设防的那张是权威表2026-08-31-查明2026-08-29-已修)。

**仍要一起发的部分**：成本公式那条 CI 断言（单价 3.0 / `per_1m_token` / 1000 token
→ 0.003）**依然没有** —— P1 无成本计算代码，写出来只能是"测试自己算一遍除法"。
且 02 §3 的 `unit_tokens` 只定义了三种按 token 的口径，而 `per_call` 实测占 15%、
NULL 也合法；两支公式已写进 02 §3，P3 实现时不得只照那个除法写。
记账从 #7/#9（均已关闭，那句"勿让 issue 关闭时漏掉"已空放过一次）移到 `gate.yml` 注释。

### 3.4 真库里有一行夹具残留，3 条 Key 断言跑在它身上（已停用+改名，仍是夹具）

`channel 1` 现名 `【夹具·勿删】verify-ui 残留（FR-094 真库靶子）`，`status=disabled`
且带停用原因；base_url 仍指向已删除的 mock 端口。**真渠道采到的 Key 数为 0，且是恒为 0**
—— `FetchKeys` 读的是上游已建的令牌列表，而不变式 N-1 禁止运行时去建
（[§3.4](P1-evidence.md#34-真库里有一行夹具残留35-项里的-3-条-key-断言跑在它身上2026-08-30-查明)）。

**不阻断**：那 3 条验的是**渲染层不吐明文**，与 Key 从哪来无关，断言本身有效。
**但要接受**：它们不是"真采集的 Key 也不回显"的证据，改名只是让这行自报身份。
真库仍是 64 真 + 1 夹具。

### 3.5 6 处形态类单测断言仍只能自我印证

`adapters_test.go` 里 `len(keys)==2`、`GroupRef=="vip"` 这类断言只能证明夹具与解析器
互相自洽（[§5.4](P1-evidence.md#54-遗留)）。本轮补了
`TestNewAPIFetchKeysPagedEnvelope`（形态取自实测），其余未动。

**不阻断**：形态的举证责任已归 `verify/pick-upstream.mjs` 的真站点校验，
且真库 35 项与真上游 58 项都在打真形态。

### 3.6 ~~文档写着 `pgx + sqlc`，而仓库里没有一行 sqlc~~ → 已按 A 结清（2026-08-29）

> **已改文档，本条不再是缺口。** 保留标题与下面的查明过程，因为"欠账怎么消失的"
> 比"欠账是什么"更值得留 —— 见末尾那条教训。

查出的事实：四处文档把存储层写成 **`pgx` + `sqlc`**，其中一处是带日期的决策行
（[10 §5 开放点 3](../dev/10-project-structure.md#5-开放点)「✅ 已定（2026-07-23）：
`pgx` + `sqlc`」），另有 [10 §2bis](../dev/10-project-structure.md#2bis-存储层查询契约m0m1-开工清单)
整节「sqlc 查询契约」。实际实现是**纯手写 pgx**：`go.mod` 无 sqlc、无 `sqlc.yaml`、
无 `queries.sql`、`Makefile` 无 `sqlc generate`。

这不是漏做一件事，是**一次没有被记录的选型翻案**。连带三处对不上账：

- [#2](https://github.com/wuhao1477/multi-upstream-ai-gateway-sla/issues/2) 与
  [#11](https://github.com/wuhao1477/multi-upstream-ai-gateway-sla/issues/11)
  **都是带着未勾选的 sqlc 完成标准关闭的**（`- [ ] sqlc generate 成功且编译通过`、
  `- [ ] sqlc generate 后无未提交 diff`）。
- [10 §5](../dev/10-project-structure.md#5-开放点) 承诺 CI 门禁「M0 后扩为 `migrations/` +
  建分区 + `sqlc generate`」，[02 §9.1bis](../dev/02-data-model.md) 门禁规则 2 同样
  写着那一步 —— 都永远不会发生。
- 新人照 10 §2bis 开工，第一步就会去找一个不存在的 `queries.sql`。

**处置（选项 A）**：改文档承认手写 pgx 胜出并写下理由。已改五处 ——

| 位置 | 改成 |
| --- | --- |
| 10 §5 开放点 3 | 标为 ⚠️ 翻案，**原决策文字整段保留**在下方翻案记录里（抹掉就读不出为什么变） |
| 10 §2bis | 整节改写：清单从 `-- name:` 注解改为"该有哪些函数"，切分原则改为收 `store.DBTX` |
| 10 §1 `store` 行 / §3 CI 门禁 / 目录树注释 | 去掉 sqlc；CI 门禁那格顺带补上实际落地的 `test-migrate.sh` 8 步与 `secret_render_test.go` |
| 02 §9.1bis 门禁规则 2 | 删掉「执行一遍 `sqlc generate`」并注明原文与删因 |
| 00 / 01 / 02 三处栈描述 | 「pgx + sqlc」→「pgx，手写 SQL」+ 指向翻案记录 |

**为什么不选 B（补 sqlc）**：10 §2bis 自己那张表就列了 sqlc 表达不了的两种形态
（事务边界、按行数分支），而本仓库现有 **7 处 `Begin(ctx)`**；再加第三条 ——
`store.DBTX` 让同一条 SQL 跑在 conn 或 tx 上，`importOne` 靠它做到 29 处调用一行未改。
补 sqlc 是给一个已跑通并被 107 项覆盖的存储层返工。

**放弃 sqlc 的代价已量化并写进翻案记录**：它把"SELECT 列数/类型与 `Scan` 参数不匹配"
从运行时错误变成编译错误，手写之后这道防线只能靠"每个查询都被真跑过"。
逐个数过：`internal/store` 带 `.Scan(` 的导出函数 **22 个，生产路径可达 22、
仅测试可达 0、无调用方 0**；`config/apply` 与 `config/history` 这两条不经界面的
由 `test-config-api.sh` 覆盖，其余由真库 35 项 + 真上游 58 项 + `test-api` 打到。
⚠️ **这个论据需要维护** —— 新增一个暂时无人调用的查询函数，就是这道防线的第一个缺口。

⚠️ **本条最值得留下的是它怎么被发现的**：不是读代码读出来的，是**对账两个已关 issue
的未勾选完成标准**。教训 —— **挂在 issue 上的欠账会随 issue 关闭一起消失**。
§3.3 那条成本公式断言也踩过同一个坑（记账从 #7/#9 移到 `gate.yml` 注释），
§3.7 的 SSRF 欠账因此直接写在本文件而不是新开 issue。

**反向也补了一次**（2026-09-01）：#2 与 #11 各留一条评论，说明那两个
`- [ ] sqlc generate …` 复选框**保持不勾**（勾上等于声称它跑过了）并指向翻案记录。
文档能追回欠账，但读 issue 的人不会先读文档 —— 指路牌得放在他站的地方。

### 3.7 采集出站请求只有最小 base_url 校验，完整 SSRF 边界挂到 RBAC（2026-08-29，2026-09-01 修正）

[Codex 二次评审](#附codex-二次评审的两条-high2026-08-29)报的第二条 [high]。

⚠️ **本节 2026-08-29 的原文是错的，2026-09-01 修正。** 原文写着：

> 已修的部分：`validateBaseURL` 收成**一处**、两个入口共用（`createChannel` 与批量导入）。

**"共用"是真的，"收齐了"是假的**，两处都假：

| 原文的说法 | 实际 | 后果 |
| --- | --- | --- |
| 入口只有两个 | 还有第三个 —— **`PATCH /admin/channels/{id}`，一处校验都没有** | POST 拒 `file:///etc/passwd`（400），PATCH 同一个值返 200 **并落库**；建渠道那道闸可被一次 PATCH 完整绕过，此后每轮 sync 都去请求它 |
| 导入侧共用了 | 共用了，但**跑在 `Detect` 之后** | "拒绝落库"拦不住**已经发出的探测请求**，而 SSRF 关心的正是那个出站请求；`dry_run=true` 更是完全走不到落库校验 |

两条都在 2026-09-01 修掉了（见[附二](#附二2026-09-01-自审--codex-三次评审的六条)①④）。校验现在真的在三个入口
之前各跑一次，守卫是 `TestPatchChannelRejectsBadBaseURL` 与
`TestImportValidatesBeforeProbing` —— 后者断言的是 **`Detect` 一次都没被调用**，
而不是"报告里标了 skipped"：后者在"先探测、探完再拒"的实现下**也会绿**。

⚠️ **这条修正本身是本轮最该记下的东西。** 那句"收成一处"不是笔误，是**我写文档时
按自己以为的实现写的**，而 107 项验收跑过 PATCH 的改名/停用/启用，**从没 PATCH 过
`base_url`** —— 于是文档、代码、验收三方各自自洽，缺口从三边中间漏过去。
教训：**"已收敛到一处"这种断言必须数入口，而入口要从路由表数，不是从记忆里数。**

**仍欠的那层**（刻意延后，不是遗漏）：解析后拒绝 loopback / 私有 / 链路本地 /
云元数据网段、自定义 `Transport` 在每次连接前复核 DNS（防 DNS rebinding）、
`CheckRedirect` 拦重定向到不允许的地址。`http://169.254.169.254/…` 是**合法 http
URL，现在照收** —— 这是设计如此，不是漏修（所以那两条守卫刻意**不**把它放进坏值列表：
混进去会让断言在"网段校验没做"时也红，而红的理由就不再是它守的那件事了）。

⚠️ **Codex 2026-09-01 三次评审按 [high] 重提这一条，判定未改。** 它的说法是
「不要把这段最小格式校验作为 SSRF 修复交付」，并建议加一个受限出站客户端
（网段黑名单 + 每次拨号复核解析后的 IP + 重定向逐跳校验）。不做的理由有三条，
前两条是原来那两个前提，第三条是这一轮新查到的事实：

1. 管理面仍是**单一 admin 令牌、无角色分级**，能改 `base_url` 的人已经握有配置面
   本身；`DetectResult` 只回结构化字段（family / version / no_shield /
   quota_per_unit），不回原始响应体，故它不是外传通道。
2. 那层加固的触发条件已经写死在这里：**引入 RBAC 或把管理面暴露到公网，立刻升级
   为阻断项。** 这不是"以后再说"，是一个有判据的挂起。
3. **真库里那行夹具渠道的 `base_url` 就是 `http://127.0.0.1:18099`**（`channel 1`，
   已停用），而 35 项验收里的 3 条 Key 断言全靠它 —— 真渠道的 Key 数恒为 0
   （不变式 N-1），这个靶子不可再生。一刀切拒绝 loopback/私网会让这个靶子**无法
   经 API 重建**，也与「把采集器指向内网地址正是本系统的用途」（真库在
   `<internal-db-host>`）相抵。
   ⚠️ 半套黑名单（只拦 `169.254`、放行 loopback/私网）**更差**：能拿到 admin 令牌
   的人照样能打 `127.0.0.1` 与 `10.0.0.0/8`，于是它买不到任何安全性，只是把
   Codex 举的那个例子从列表里划掉 —— 那是给评审看的，不是给攻击者看的。

**判定影响**：不改①~④任何一条（这一层从来不在四条退出标准里）。它仍是 §3 的一条
已知缺口，且是唯一一条**外部评审两轮都判 [high]、而我两轮都判不阻断**的。
读到这里的人应该知道：这是一处**明确的判定分歧**，不是一处被忽略的发现。


**为什么不阻断发版**：管理面是**单一 admin 令牌、无角色分级**（全仓 `internal/admin`
无 role/permission 概念），且 `DetectResult` 只回结构化字段（family / version /
no_shield / quota_per_unit），**不回原始响应体** —— 故它不是外传通道。能改 `base_url`
的人已经握有配置面本身；而"把采集器指向内网地址"正是本系统的**用途**（真库就在
`<internal-db-host>`）。

⚠️ **引入 RBAC 或把管理面暴露到公网时，这条立刻升级为阻断项** —— 那时存在一个
"能配渠道但不该能探内网"的角色，上面那层就成了唯一屏障。记在这里而不是某个 issue：
#2/#9 的教训是**挂在 issue 上的欠账会随 issue 关闭一起消失**（§3.6 正是这样翻出来的）。

### 3.8 72 个配置键里 68 个没有任何代码读，而界面上看不出来（2026-09-01 查明）

配置面本身是好的（两步确认、乐观锁、版本历史、幂等去重、`test-config-api.sh` 9+2
项端到端全绿）。缺的是**一句话**：改一个还没接线的键，什么都不会发生。

| | |
| --- | --- |
| 被消费的 | **4 个** —— `collector_request_interval_ms`（`cmd/sla-core`、`cmd/collector`）、`collector_catalog_interval_h`、`catalog_missing_rounds`、`sync_min_interval_s`（后三个在 `internal/admin`） |
| 未被消费的 | **68 个** —— 调度期限、探测预算、容量上限、余额安全垫、健康与冷却、计费容差、告警生命周期、账本批提交、debug 抓包…… 全部属 P2~P4 |
| 判据 | 遍历 `params_gen.go` 的 72 个 `Key:`，在非生成、非测试的 Go 源里搜 `"<key>"`；68 个零命中 |
| 现在的表现 | 运维改 `max_hops`：走完两步确认 → 落 `config_params` → 进版本历史 → **零行为变化**。接口与界面都不说这件事 |

**为什么不删这 68 个键**：[09 §4bis](../dev/09-admin-api.md) 的设计就是"先登记其存在"
—— 一个键的默认值与是否关键项，是 P2 决策路径的输入契约，删了就得在接线时重新论证
一遍（而那份论证已经写在 09 里了）。这与 §3.6 那条"放弃 sqlc 的编译期检查"是同类
取舍：留着有维护成本，删掉的重建成本更高。

**为什么也不加"未接线"标记到界面**：那需要一份"哪些键已接线"的清单，而**那份清单
就是第二处真相** —— 它会与代码分叉，而分叉的方向永远是"忘了更新"。同 §3.6 那条
「不写死家族清单」的理由。真正管得住的判据是上面那条命令，写在这里。

**不阻断**：一期没有任何 AC 依赖这 68 个键；四条退出标准也不涉及。它的后果是**运维
可能以为自己调了参数**，而这条现在写下来了。⚠️ P2 接线时逐个键要做的事：接上 →
从这张表的"未被消费"里划掉。**这张表的两个数字加起来必须永远是 72。**


### ~~① 那 20 个站的维护责任人~~ → 已决定：全部放弃纳管（2026-08-31）

三类（需人工重登 / 形态不符 / 凭证失效）全部放弃，20 个渠道已 `status=disabled` +
停用原因，责任人一列作废。执行方式与撞出来的缺陷见
[§5.18](P1-evidence.md#518-放弃-20-个采不到的站并修掉停用不影响采集2026-08-31)。

它带出的连带后果 —— 那 20 个装满了全部 13 个 sub2api 渠道，于是 **AC-38 在真库上
不可复验** —— 2026-08-31 曾按"认首轮归档为终局证据"结清，**2026-09-01 撤销**：
判定基准仍不动，但 AC-38 与退出标准①记为"不可判定 → 未通过"（§3.1、§1）。

### ~~② `channel 1` 那行夹具残留清不清~~ → 已决定：停用 + 改名，不清除（2026-08-29）

原候选是"清掉（降到 32 项）"与"留着不动"。执行时取第三种，因为查清了一个之前
没写下来的事实：**真渠道的 Key 数恒为 0，不是"还没采到"** —— 于是清除是**永久**
销毁那 3 条断言的唯一靶子，代价比原先估的"降 3 项"更重。改名让这行自报身份，
停用让它退出在纳管台账（44 在纳管 / 21 停用）并停止每轮产一条采集 fatal。
执行与反向验证（`{"status":"disabled"}` 无原因 → HTTP 400）见
[§3.4](P1-evidence.md#34-真库里有一行夹具残留35-项里的-3-条-key-断言跑在它身上2026-08-30-查明)。

### ~~③ 文档里那条 `pgx + sqlc` 决策怎么结清~~ → 已决定：选 A，改文档（2026-08-29）

三个候选是 **A 改文档承认手写 pgx 胜出**、B 回头补 sqlc、C 原样发。选 A 并已执行：
改动清单与"为什么不选 B"见 §3.6，翻案记录（含**原决策文字整段保留**）落在
[10 §5 开放点 3](../dev/10-project-structure.md#5-开放点)。

⚠️ 结清后新增一条**要维护的论据**：放弃 sqlc 等于放弃"列数/类型与 `Scan` 不匹配"的
编译期检查，代之以"22 个带 `.Scan(` 的导出函数全部生产路径可达"。**新增一个暂时
无人调用的查询函数，就是这道防线的第一个缺口** —— 这句话同时写在 10 §5 里。

## 5. 发版建议

**这份文件不再说"可以发版"。** 它说的是：**技术面按现状可发，而退出标准①按本仓库
自己的规则未满足** —— 发不发是你的决定。两条路都成立，代价不同：

| 选项 | 代价 |
| --- | --- |
| **照发**（推荐） | 退出标准①带着"不可判定"发出去。必须在发布说明里**写明**，不能只写 ✅ —— 否则读的人以为 AC-38 在 HEAD 上验过 |
| 先补齐① | 真库里重新纳管一个 sub2api 渠道并跑通 AC-38。**与"放弃 20 个站"这个运营决定直接冲突**，所以这不是加个待办能解决的，是推翻一个已执行的决定 |

照发的话，PR/发布说明里带上：

1. **退出标准①是"不可判定 → 未通过"**，以及原因（AC-38 的 sub2api 一族在 HEAD 上跑不了）；
2. 本文件 §3 的六条缺口原文（不要压缩成"已知小问题"）；
3. 「107 项验收中 93 项只能本地跑」这个事实，以及原因；
4. [P1-manual-channels.md](P1-manual-channels.md) 的链接，注明**那 20 个渠道已放弃纳管**
   （不是"责任人待指派"），以及 AC-38 因此不可复验这件事；
5. 「分支不合并」这个当前状态 —— 2026-08-31 决定，发版口径与合并时机分开决定。

**不建议在发版前做的事**：为了让 CI 数字好看而给上游/真库加 mock。那样得到的绿是假的，
而真实覆盖仍然为零 —— 这条是本仓库自己撞出来的规则（[CLAUDE.md §1](../../CLAUDE.md)），
不是风格偏好。**同理不建议把①改回 ✅** —— 那个 ✅ 是本轮改判要拿掉的东西。

## 附：Codex 二次评审的两条 [high]（2026-08-29）

以 `gpt-5.6-sol` 跑 `adversarial-review`（前两次分别因 `INSUFFICIENT_BALANCE` 与
`stream disconnected` 中断，第三次 exit 0）。它报了两条，**都不在评审前那六条缺口
（§3.1~§3.6）里，也都没被 107 项验收抓到** —— 值得记下来的正是这一点：那 107 项是按
"功能是否可用"组织的，而这两条是"失败路径下会留下什么"。

| # | 结论 | 处置 |
| --- | --- | --- |
| ① 导入失败留下不可自愈半成品 | **确认，已修** | 四处写入收进一个 `pgx.Tx`；重试能补齐半成品；3 条真库测试 + 3 路反向自验（§2 表内 `test-migrate.sh` 8/8） |
| ② 采集出站无 SSRF 边界 | **机制确认，档位下调** | 校验收成一处、两入口共用（已修）；网段/DNS/重定向那层挂到 RBAC，见 §3.7 |

**它查到的、我自己那轮复评漏掉的**：`SaveDetected` / `SaveCredential` 是注入函数，
**各自 `Pool.Acquire` 取另一条连接独立提交** —— 所以"给 `conn` 包一层事务"根本盖不住
它们。这是本轮改动里最实质的一处：`store.DBTX` 接口（`*pgx.Conn` 与 `pgx.Tx` 都已满足，
故 29 处现有调用一行未改）+ `Save` / `SaveTx` 拆成两个（续期路径无外层事务，自取连接
是对的；导入路径必须同生共死）。

**我与它判定不同的一处**：②它给 `high`，我给"发版不阻断"。依据是管理面无角色分级、
`DetectResult` 不回原始 body。⚠️ 这个下调**绑定在那两个前提上** —— 前提一变（RBAC /
公网暴露）就得重新判，§3.7 末尾写明了。

⚠️ 反向自验里查出的**测试自身缺陷**：三条真库测试第一版**不幂等** —— 破坏自验跑完
留下 7 行残渣，下一轮就红在"回滚不彻底"上，而那时被测代码是好的。已加 `wipe()`
在每条**开跑前**清自己的 `base_url`；且它必须逐张删子表，因为 `channels` 的外键
**多数是 NO ACTION 而非 CASCADE**（第一版直接 `DELETE FROM channels` 撞了
`upstream_accounts_channel_id_fkey`，而那句错误当时被 `_, _ =` 吞掉，
表现成"清了但没清掉"）。同 [§5.21](P1-evidence.md#521-key-明文那条退出标准在-ci-里零覆盖而补上的守卫头两条是空的2026-08-29)
那条教训：**破坏测试自己也要被怀疑。**

## 附二：2026-09-01 自审 + Codex 三次评审的六条

流程是你要的三段：**我先审一遍 → Codex 再审一遍 → 再深入审**。结果是
**六条缺陷，我查到 4 条、Codex 查到 5 条、重叠 3 条** —— 重叠的那 3 条里有 2 条
（①④）同时**证伪了我自己写在 §3.7 与一条提交说明里的话**，这是本轮最有价值的产出：
不是"又修了几个 bug"，而是**一份文档里的自我表扬被自己的复审推翻了**。

| # | 缺陷 | 谁查到 | 档位 | 处置 | 守卫（全部打真 PG） |
| --- | --- | --- | --- | --- | --- |
| ① | `PATCH /admin/channels/{id}` 的 `base_url` **一处校验都没有** | 我 + Codex | Codex 判 [critical] | 已修：与 POST 共用 `validateBaseURL` + 同样去尾斜杠 | `TestPatchChannelRejectsBadBaseURL` |
| ② | 建渠道与探测快照**不原子** —— `SaveDetected` 失败只 Warn，接口照样 201 | Codex | [high] | 已修：整笔收进 `pgx.Tx`，落库失败则回滚且不返 201 | `TestCreateChannelRollsBackWhenSaveDetectedFails` |
| ③ | 渠道身份**无库级唯一性**，一个尾斜杠就能建出两行同址渠道 | 我 + Codex | — | 已修：迁移 **019** 加 `UNIQUE(base_url)`；写入侧统一去尾斜杠；23505 → `ErrDuplicate` → HTTP 409 | `TestChannelBaseURLNormalizedAndUnique` |
| ④ | 导入侧校验跑在 `Detect` **之后**，出站请求已经发了 | 我 + Codex | Codex 判 [high] | 已修：校验挪到探测之前，`dry_run` 也走同一道 | `TestImportValidatesBeforeProbing` |
| ⑤ | 补齐半成品**不补探测快照** → 报 `imported` 而那个渠道一次都采不了 | 我 | — | 已修：`incompleteParts` 加查 `__detect__` 快照，`repairChannel` 能补 | `TestImportRepairAddsSnapshotAndKeepsWarning` |
| ⑥ | 补齐分支**整句覆盖** `warning` → `shielded_sites` / `without_credential` 少计 | Codex | — | 已修：改成追加 | 同上（一条测两件） |

八条真库测试挂在 `test-migrate.sh` 第 8 步（`EXPECT=8`，写死数目是刻意的：
`-run` 正则打错一个字就少匹配几条，而日志里看不出来）。

**反向自验：7 路，全部红在自己那句断言上**（`verify/break-review-fixes.py`，用完即删）。
它自己的四个坑值得单独记，因为**每一个都会产出假绿**：

1. **`git checkout --` 还原 = 把修复本身删掉。** 被测的修复全是未提交的工作树改动。
   第一路破坏后还原，`upstream_api.go` 退回 HEAD，后面两路在**旧代码**上跑照样打 ✅ ——
   三条假绿，而修复被删了。改成开跑前把字节存进内存，还原就是写回去。
2. **破坏没生效也会打 ✅。** 目标字符串缩进对不上就一个字都没替换。现在每处替换
   断言**命中次数恰好为 1**，不满足抛 `HARNESS:` 而不是继续。
3. **"把写入从 `tx` 挪回裸 `conn`"不是一个破坏。** pgx 里 conn 上开了 tx 之后，
   直接用 conn 执行的语句**仍在那个事务里**，回滚照样带走它 —— 测试仍绿，
   而那不是断言无效，是**破坏无效**。换成"忽略 `SaveDetected` 的错误"才真的红。
4. **还原留残渣 → 下一轮红在无关的理由上。** 无约束那轮真的插进了第二行，带着
   重复行 `ADD CONSTRAINT` 会失败，而返回码没查 —— 于是收尾那次"干净树全绿"
   红在"同一个地址落了 2 行"上，看起来像修复失效。现在先清子表与重复行，
   且非零退出即抛 `HARNESS:`。

⚠️ 同一条老教训第三次出现：**断言绿 ≠ 断言有效**，而**破坏脚本自己也要自证生效**。
前两次分别是 §5.21（守卫"形状对、范围错"）与附一末尾（测试不幂等）。

**判定影响**：这六条**都不影响四条退出标准的判定** —— ①④ 是 §3.7 那条已知欠账的
边界被画错，②③⑤⑥ 是失败路径下的残留。①的改判（✅ → 不可判定）与本轮六条**无关**，
它来自 Codex 对 14 §3 规则 3 的引用，见 [§1](#1-四条退出标准的判定)。


## 附三：2026-09-01 第三轮 —— 这次审的是「有没有过度设计」

前两轮问的是"哪里错了"，这轮问的是**"哪里可以删"**。我先按 ponytail 那把尺子过一遍
（只看过度优化 / 过度设计 / 无用代码，正确性与安全另算），再让 Codex 以同样的主轴
对抗性复审一次。我报 6 条、Codex 报 3 条，重叠 1 条。

### 删掉的

| # | 位置 | 删了什么 | 换成什么 | 谁提 |
| --- | --- | --- | --- | --- |
| ① | `import_api.go` 的 `detectStash` | 带 mutex 的 map + 两个方法 + 一段注释（-28 行） | `make([]DetectResult, len(accounts))`，按下标写。它与紧邻的 `res.Items[idx] = item` 做的是同一件事，而后者从来没上锁 —— 那把锁不保护任何东西（`-race` 全绿证明了这点）。顺带删掉 `load` 的 `ok` 分支：`status=="detected"` 时它恒真 | 我 |
| ② | `validateBaseURL` + 三处 `TrimRight` | 校验与规范化分离的四处代码 | **合成一个函数**：`validateBaseURL` 现在返回规范形态，三个入口各一次调用。原先三份规范化里**两份漏了 `TrimSpace`** —— 首尾带空白的 `site_url` 在探测那头放行（请求真的发出去了）、到落库那头判 400，同一个输入两种判定 | 我 + Codex |
| ③ | `test-migrate.sh` 第 8 步的 `-run` 前缀正则 | `TestImport\|TestChannel\|…` | 逐个列出并锚定 `^(…)$` 的 8 个测试名。前缀正则会让**任何**日后新增的同前缀合法测试把迁移验收弄红，而那跟迁移毫无关系 | Codex [low] |
| ④ | `cmd/collector` 的一行日志 | 「采集适配器尚未接入（#5/#6/#7）」—— **已经不成立**，那三个 issue 早已完成 | 说真话：一期无周期采集（FR-116 属 P2 起），采集经管理面手动触发（FR-128）。留一句指向已关 issue 的话，会让人去翻三个已完成的 issue 找原因 | 我 |

②同时修掉 Codex 的 [medium]：`validateBaseURL` 现在**小写 host**（不动 path），于是
`https://EXAMPLE.invalid` 与 `https://example.invalid` 规范成同一个字面值，被 019 拦住。
019 的头部原先把这条记成"残留缺口"，那是记错了 —— 它不必残留，正确位置在写入侧而非约束侧。
守卫：`TestChannelBaseURLNormalizedAndUnique` 加了大写 host 那一段（该测试现在清**三种**拼法，
否则失败一次就留一行残渣毒掉下一轮）。

### 没删的，以及为什么

| 提议 | 判定 | 理由 |
| --- | --- | --- |
| 加受限出站客户端（网段黑名单 + 每次拨号复核 IP + 重定向校验），Codex [high] | **不做** | 三条理由写在 §3.7，其中第三条是这轮新查到的：真库那行夹具渠道的 `base_url` 就是 `http://127.0.0.1:18099`，35 项里 3 条 Key 断言全靠它，而真渠道 Key 数恒为 0（靶子不可再生）。半套黑名单更差 —— 买不到安全性，只是把 Codex 举的例子划掉 |
| `base_url` 建 canonical 列 + 在其上建唯一索引，Codex [medium] 的后半 | **不做** | 真库 65 行里大小写重复 **0 行**，写入侧规范化已让约束生效。加一列就多一处要与写入侧同步的真相，而它防的是一个当前不存在的形态 |
| 删 `test-migrate.sh` 的 `EXPECT` 固定数，Codex [low] 的后半 | **不做**（改成随列表长度派生） | `-run` 里打错一个字的那条只是匹配不上，`go test` 照样 exit 0 并打 `no tests to run` —— 那正是最容易悄悄发生的假绿，而它恰恰**不会**被退出码抓到。列表管"别牵连无关测试"，计数管"列表本身别写错" |
| 删 `cmd/collector` 整个二进制 + compose 的 collector 服务（我自己提的） | **不做** | 它是 [06 §1](../dev/06-deployment-and-operations.md) 拓扑的一部分、`-once` 被 `verify/docker-compose.arm.yml` 传着，删它要动 5 个文件并改一条通过中的验收断言。这是范围决定，不是清理 |
| 删 `test-config-api.sh` 里我两小时前刚加的 PG 就绪守卫（我自己提的） | **不做** | 提的时候以为它与 core 那条重复。但反向自验时实测到一种 core 日志为空的形态（端口有人监听但不说话，pgx 卡在 ping），那时只有这条能说话 |
| 删那 68 个没接线的配置键 | **不做** | 见 [§3.8](#38-72-个配置键里-68-个没有任何代码读而界面上看不出来2026-09-01-查明)：删了在 P2 接线时要重新论证一遍，而论证已经写在 09 里。改成把事实写下来 + 给一条可复跑的判据 |

净效果 **−40 行产品代码**，外加一条新记下来的缺口（§3.8）。⚠️ 值得记的是这一轮的
**命中率**：我报的 6 条里有 2 条（`cmd/collector`、PG 守卫）自己审着审着就被自己驳回了 ——
"能删"和"该删"不是一回事，而写下驳回理由比删掉它更有用。

### 清理之后自己撞出来的三条（都不是被审出来的）

三条都在**验证链**上，一条也不在产品代码里。写下来是因为它们有共同点：
**本地全绿、而且是"改动看起来无害"的那种改动。**

| # | 症状 | 真因 | 修法 |
| --- | --- | --- | --- |
| ⑤ | `remote-stack.sh` 起不来 core：「迁移 019 已应用但内容已变（库中 `e9b2a887…`，当前 `4b6a88f2…`）」 | 上面②那条修复顺手**改了 019 的注释**，而 019 早在本轮已应用到真库。checksum 覆盖**整个文件**，注释也算 —— 这正是 019 自己头部记的那条契约，只不过它写的是"所以不要回去改 001" | 文件回滚到那 16 位 checksum 对应的字节（body 本就没动），更正搬到 [02 §channels](../dev/02-data-model.md) 的 `base_url` 注释旁 —— 那里没有 checksum 契约。并在那里写明 019 头部那句已过时且**改不了**，以 02 为准 |
| ⑥ | CI `gate` 红在 `docker run`：`failed to bind host port for 0.0.0.0:55434 … address already in use`（run #33432698039） | 全仓只有 `test-config-api.sh` 用 55434，也不是容器没清 —— 55434 落在 **Linux 默认临时端口段**（32768–60999）里，同 job 早前步骤（`go mod` / `pnpm` / `docker pull`）的任一条出站连接都可能正借着它，`docker-proxy` 随后绑不上 | 四个 PG 宿主端口全挪到 184xx：`test-migrate` 55433→18433 / `test-config-api` 55434→18434 / `ui-stack` 55441→18441、55442→18442。core 那几个口本来就是 18xxx，所以这坑**只在 PG 上冒过** |
| ⑦ | CI `gate` 红在 `TimeoutError: Timed out after 30000 ms while waiting for the WS endpoint URL`（run #33438992013） | 不是不兼容：`--no-sandbox` 与 `--disable-dev-shm-usage` 本来就在，且**同一个 runner 镜像五分钟前那轮（#33438576085）同一步是绿的**。就是慢 —— 而 30000 是 puppeteer 的默认值，不是谁掂量过的选择 | 三处 `puppeteer.launch` 各加一行 `timeout: 90_000`（理由写在 `verify-spa.mjs` 一处，另两处指过去）。真慢时才花掉 |

⚠️ **⑤是"我自己刚写下那条契约、又立刻违反它"** —— 019 的头部专门解释了为什么不能回去改
001，而我回去改了 019。契约里没写"注释除外"，因为 checksum 也看不出来那是注释。

⚠️ **⑥⑦揭示的是同一件事：本地验证链抓不到"只在别人机器上才成立"的前提。**
⑤同理 —— `test-migrate.sh` 每轮新建空库，"已经有这个迁移的库"这个前提只有真库满足，
所以抓到⑤的是那 35 项，不是 8/8。三条都不该记成"运气"：⑥⑦的真因都在**默认值**上
（临时端口段是内核默认、30s 是 puppeteer 默认），而默认值在笔记本上刚好都够用。
