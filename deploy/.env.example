# 复制为 deploy/.env 后填写。**.env 已在 .gitignore，绝不入库。**
#
# 分工（06 §4）：
#   · 基础设施配置（本文件）→ 环境变量
#   · 业务策略参数          → PG 的 config_params，经 /admin/config 改（09）
# 两者不混：策略要版本化、要二次确认、要审计，env 给不了这些。

# ── 必填 ──

# 发布镜像部署时使用的镜像；源码构建验收可保持默认值。
SLA_IMAGE=ghcr.io/wuhao1477/multi-upstream-ai-gateway-sla-public:v1.0.0

# 管理平面令牌。**不落 config_params**（09 §4bis：避免自己改自己 ——
# 若它在表里，管理 API 就能改自己的鉴权令牌 = 提权）。
# 生成：openssl rand -hex 32
ADMIN_TOKEN=

# PG 密码。仅容器网络可达，但仍不要用默认值。
POSTGRES_PASSWORD=

# 本机管理 UI/API 端口。仅绑定 127.0.0.1，不经 Caddy 对外暴露。
ADMIN_PORT=8080

# Caddy 站点名；localhost 使用自签证书。
SITE_ADDRESS=localhost

# ── 上游渠道凭证的说明（P1 起用到）──
#
# ⚠️ 上游 `sk-` key 与采集凭证**不放这里**，而是登记在 PG 的
#    upstream_keys / collector_credentials（一期明文，FR-113），
#    经 /admin/keys 与 /admin/collector/credentials 写入。
#    理由：它们是**运行时数据**（可增删改、有状态机、要轮换），
#    不是部署配置；放 env 会导致加一个渠道就要重启。
#
# 唯一的例外是 M1 基线测试站点的 key（15 §1bis）——那是测试夹具而非
# 运行时凭证，届时按需在此追加，且同样不入库。
